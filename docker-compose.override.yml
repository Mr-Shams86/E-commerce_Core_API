services:
  api:
    volumes:
      - ./app:/app/app
      - ./alembic:/app/alembic
      - ./alembic.ini:/app/alembic.ini
    # (опционально, удобно для разработки)
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --proxy-headers --reload
